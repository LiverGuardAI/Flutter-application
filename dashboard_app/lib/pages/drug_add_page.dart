import 'package:flutter/material.dart';

class DrugAddPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("복용약물 추가")),
      body: Center(child: Text("복용약물 추가 페이지")),
    );
  }
}
